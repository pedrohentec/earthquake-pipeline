version: 2

sources:
  - name: raw
    description: "Raw earthquake data from USGS via MinIO (local Parquet files)"
    tables:
      - name: earthquakes
        description: "Raw earthquake events partitioned by year/month/day"